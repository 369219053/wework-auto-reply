# 🤖 Hamibot使用教程

## 📖 目录

1. [Hamibot介绍](#hamibot介绍)
2. [安装和配置](#安装和配置)
3. [脚本导入和配置](#脚本导入和配置)
4. [运行和测试](#运行和测试)
5. [高级配置](#高级配置)
6. [故障排查](#故障排查)

---

## 🤖 Hamibot介绍

### 什么是Hamibot?

Hamibot是一款基于Auto.js的Android自动化平台,支持使用JavaScript编写自动化脚本。

### 核心优势

- ✅ **功能强大** - 支持复杂的自动化逻辑
- ✅ **云端管理** - 可以远程更新脚本
- ✅ **社区支持** - 有丰富的脚本库和教程
- ✅ **开发简单** - 使用JavaScript编写

### 与MacroDroid对比

| 对比项 | MacroDroid | Hamibot |
|--------|-----------|---------|
| 配置难度 | ⭐ | ⭐⭐⭐ |
| 功能强大度 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 成本 | 免费 | ¥30/月 |
| 适合人群 | 普通用户 | 有编程基础 |

---

## 🔧 安装和配置

### 第一步: 下载安装Hamibot

#### 方法1: 官网下载(推荐)
```
1. 访问: https://hamibot.com/
2. 点击"下载"
3. 下载最新版APK
4. 安装APK
```

#### 方法2: 应用商店
```
部分应用商店可能有Hamibot
搜索"Hamibot"并安装
```

### 第二步: 注册账号

```
1. 打开Hamibot应用
2. 点击"注册"
3. 输入手机号/邮箱
4. 设置密码
5. 完成注册
```

### 第三步: 授予权限

#### 1. 无障碍权限(必须)

```
Hamibot → 设置 → 无障碍服务
→ 开启无障碍权限
```

#### 2. 悬浮窗权限(必须)

```
Hamibot → 设置 → 悬浮窗权限
→ 允许显示悬浮窗
```

#### 3. 存储权限(必须)

```
Hamibot → 设置 → 存储权限
→ 允许访问存储
```

### 第四步: 购买会员(可选)

```
免费版限制:
- 最多3个脚本
- 部分功能受限

付费版(¥30/月):
- 无限脚本
- 全部功能
- 云端同步
```

---

## 📝 脚本导入和配置

### 方法1: 导入本地脚本文件

```
1. 将"企微自动回复.js"复制到手机
2. 打开Hamibot
3. 点击"+"新建脚本
4. 选择"导入本地文件"
5. 选择"企微自动回复.js"
6. 完成导入
```

### 方法2: 手动创建脚本

```
1. 打开Hamibot
2. 点击"+"新建脚本
3. 输入脚本名称: "企微自动回复"
4. 复制脚本代码粘贴进去
5. 保存
```

### 配置脚本参数

打开脚本,找到配置区域,修改以下参数:

#### 1. 欢迎语配置

```javascript
welcomeMessage: "您好!欢迎添加,请点击链接加入我们的群聊: [您的群聊链接]"
```

**重要:** 将`[您的群聊链接]`替换为实际的群聊邀请链接!

#### 2. 通知关键词配置

```javascript
notificationKeywords: ["新的朋友", "添加了你", "通过了你的好友申请"]
```

**说明:** 可以添加更多关键词,用逗号分隔

#### 3. 延迟时间配置

```javascript
delays: {
    afterNotification: 2000,  // 收到通知后等待2秒
    afterClick: 1500,         // 点击通知后等待1.5秒
    afterInput: 500,          // 输入文本后等待0.5秒
    beforeReturn: 1000        // 返回主屏幕前等待1秒
}
```

**建议:** 如果执行不稳定,可以适当增加延迟时间

#### 4. 工作时间限制(可选)

```javascript
workingHours: {
    enabled: true,            // 启用工作时间限制
    start: 9,                 // 早上9点开始
    end: 18                   // 下午6点结束
}
```

**说明:** 
- `enabled: false` - 全天运行
- `enabled: true` - 只在指定时间运行

#### 5. 日志配置

```javascript
logging: {
    enabled: true,            // 启用日志
    showToast: true           // 显示Toast提示
}
```

---

## 🚀 运行和测试

### 启动脚本

```
1. 打开Hamibot
2. 找到"企微自动回复"脚本
3. 点击"运行"按钮
4. 脚本开始运行
```

### 查看运行状态

```
运行中会显示:
- 悬浮窗图标(表示脚本正在运行)
- 日志输出(在Hamibot控制台)
- Toast提示(如果启用)
```

### 测试脚本

```
1. 让朋友添加您的企业微信
2. 观察Hamibot是否有日志输出
3. 检查是否自动发送了欢迎消息
```

### 查看日志

```
Hamibot → 点击脚本 → 查看日志
可以看到:
- 脚本启动信息
- 收到通知信息
- 发送消息信息
- 错误信息(如有)
```

### 停止脚本

```
方法1: 点击悬浮窗的停止按钮
方法2: 在Hamibot中点击"停止"
方法3: 下拉通知栏,点击"停止脚本"
```

---

## 🎯 高级配置

### 1. 根据时间发送不同欢迎语

修改脚本,在`sendWelcomeMessage`函数中添加:

```javascript
function getWelcomeMessage() {
    const hour = new Date().getHours();
    
    if (hour >= 6 && hour < 12) {
        return "早上好!欢迎添加,请点击链接加入我们的群聊: [链接]";
    } else if (hour >= 12 && hour < 18) {
        return "下午好!欢迎添加,请点击链接加入我们的群聊: [链接]";
    } else {
        return "晚上好!欢迎添加,请点击链接加入我们的群聊: [链接]";
    }
}

// 在发送消息时使用
inputBox.setText(getWelcomeMessage());
```

### 2. 记录每次自动回复

在`sendWelcomeMessage`函数中添加:

```javascript
// 记录到文件
const logFile = "/sdcard/企微自动回复日志.txt";
const logContent = new Date().toLocaleString() + " - 已发送欢迎消息\n";
files.append(logFile, logContent);
```

### 3. 发送图片或小程序

```javascript
// 发送图片
const imageBtn = desc("图片").findOne(3000);
if (imageBtn) {
    imageBtn.click();
    // 选择图片...
}

// 发送小程序
const miniProgramBtn = desc("小程序").findOne(3000);
if (miniProgramBtn) {
    miniProgramBtn.click();
    // 选择小程序...
}
```

### 4. 云端配置管理

使用Hamibot的云端配置功能:

```javascript
// 从云端获取配置
const cloudConfig = hamibot.getConfig();
const welcomeMessage = cloudConfig.welcomeMessage || "默认欢迎语";
```

---

## 🔧 故障排查

### 问题1: 脚本无法运行

**可能原因:**
1. 无障碍权限未授予
2. 悬浮窗权限未授予
3. 脚本有语法错误

**解决方法:**
```
1. 检查所有权限是否已授予
2. 查看日志中的错误信息
3. 检查脚本代码是否正确
```

### 问题2: 无法监听通知

**可能原因:**
1. 通知权限未授予
2. 企业微信通知被关闭

**解决方法:**
```
1. 检查Hamibot的通知权限
2. 检查企业微信的通知设置
3. 重启Hamibot
```

### 问题3: 找不到输入框

**可能原因:**
1. 等待时间太短
2. 企业微信界面改版
3. 元素定位不准确

**解决方法:**
```
1. 增加延迟时间
2. 使用UI分析工具查看元素
3. 更新元素定位代码
```

### 问题4: 脚本自动停止

**可能原因:**
1. 手机清理了后台
2. 电池优化限制
3. 脚本出错退出

**解决方法:**
```
1. 将Hamibot加入自启动白名单
2. 关闭电池优化
3. 查看日志找出错误原因
```

---

## 📚 更多资源

- [Hamibot官方文档](https://docs.hamibot.com/)
- [Auto.js文档](https://hyb1996.github.io/AutoJs-Docs/)
- [Hamibot社区](https://hamibot.com/community)

---

## 💡 提示

### 优化建议

1. **合理设置延迟时间** - 避免操作过快导致失败
2. **启用日志记录** - 便于排查问题
3. **定期检查脚本** - 企业微信更新后可能需要调整
4. **备份脚本** - 定期备份到云端

### 注意事项

- 🚨 不要频繁操作,避免被检测
- 🚨 保持Hamibot在后台运行
- 🚨 定期查看日志,确保正常运行
- 🚨 企业微信更新后及时测试

---

[返回README](../README.md)

