# 批量发送功能 - 常见问题与解决方法

## 📋 目录

1. [发送按钮点击问题](#发送按钮点击问题)
2. [联系人查找问题](#联系人查找问题)
3. [多选模式滚动问题](#多选模式滚动问题)
4. [逐条转发按钮点击问题](#逐条转发按钮点击问题)
5. [功能二执行功能一脚本问题](#功能二执行功能一脚本问题)
6. [消息节点识别问题](#消息节点识别问题)

---

## 🔧 发送按钮点击问题

### 问题描述

在发送确认弹窗中,点击"发送"按钮时,`clickNode()`返回`true`但实际没有点击成功。

### 问题原因

1. **错误的节点查找**: 使用`findNodeContainingText(rootNode, "发送")`会找到错误的节点
   - 找到的是"发送给："标题文本,而不是"发送"按钮
   - 标题文本节点不可点击

2. **点击方法不可靠**: `clickNode()`在某些情况下返回`true`但实际没有触发点击

### 解决方案

#### 方案1: 精确查找按钮节点 ✅

使用`resource-id`精确查找"发送"按钮:

```kotlin
// ❌ 错误方法
val sendButton = findNodeContainingText(rootNode, "发送")

// ✅ 正确方法
val sendButton = findNodeByResourceId(rootNode, "com.tencent.wework:id/dbo")
```

#### 方案2: 双重点击策略 ✅

实现主方法+备用方案的双重保险:

```kotlin
// 方法1: performAction(ACTION_CLICK) - 优先尝试
val actionClicked = confirmButton.performAction(AccessibilityNodeInfo.ACTION_CLICK)

if (actionClicked) {
    // 等待500ms检查弹窗是否关闭
    handler.postDelayed({
        val stillHasDialog = rootNode?.let { 
            findNodeByResourceId(it, "com.tencent.wework:id/dbo") != null 
        } ?: false
        
        if (stillHasDialog) {
            // 方法1失败,使用方法2: 坐标点击
            clickSendButtonByCoordinate(confirmButton, targetChat)
        } else {
            // 方法1成功
            onSendSuccess(targetChat)
        }
    }, 500)
} else {
    // 方法1失败,直接使用方法2
    clickSendButtonByCoordinate(confirmButton, targetChat)
}
```

#### 方法2: 坐标点击实现

```kotlin
private fun clickSendButtonByCoordinate(confirmButton: AccessibilityNodeInfo, targetChat: String) {
    val rect = android.graphics.Rect()
    confirmButton.getBoundsInScreen(rect)
    
    val centerX = (rect.left + rect.right) / 2
    val centerY = (rect.top + rect.bottom) / 2
    
    val path = android.graphics.Path()
    path.moveTo(centerX.toFloat(), centerY.toFloat())
    
    val gestureBuilder = android.accessibilityservice.GestureDescription.Builder()
    val strokeDescription = android.accessibilityservice.GestureDescription.StrokeDescription(path, 0, 100)
    gestureBuilder.addStroke(strokeDescription)
    
    dispatchGesture(gestureBuilder.build(), object : GestureResultCallback() {
        override fun onCompleted(gestureDescription: GestureDescription?) {
            onSendSuccess(targetChat)
        }
        override fun onCancelled(gestureDescription: GestureDescription?) {
            onSendFailed(targetChat)
        }
    }, null)
}
```

### 关键技术点

1. **精确节点查找**: 使用`resource-id`而不是文本查找
2. **双重保险**: 主方法失败时自动切换到备用方案
3. **动态坐标计算**: 不硬编码坐标,而是根据按钮实际位置计算
4. **智能验证**: 点击后检查弹窗是否关闭,确认点击成功

---

## 🔍 联系人查找问题

### 问题描述

在选择联系人页面,第二个联系人"天天一泉～小石榴"在页面下方不可见,导致`findNodeContainingText()`找不到。

### 问题原因

1. **可见性限制**: `findNodeContainingText()`只能查找当前可见区域的节点
2. **字符编码差异**: 数据库存储的是半角波浪号`~`,页面显示的是全角波浪号`～`

### 解决方案

#### 方案1: 滚动查找机制 ✅

实现智能滚动,直到找到目标联系人或滚动到底部:

```kotlin
private fun selectTargetChat(scrollAttempts: Int = 0) {
    val targetChat = groupChats[currentChatIndex]
    val rootNode = rootInActiveWindow ?: return
    
    // 查找目标联系人
    val chatNode = findNodeContainingText(rootNode, targetChat)
    
    if (chatNode != null) {
        // 找到了,点击
        clickChatNode(chatNode, targetChat)
    } else {
        // 未找到,尝试向下滚动
        val scrollableNode = findScrollableNode(rootNode)
        if (scrollableNode != null) {
            val scrolled = scrollableNode.performAction(AccessibilityNodeInfo.ACTION_SCROLL_FORWARD)
            
            if (scrolled) {
                // 滚动成功,等待后继续查找
                handler.postDelayed({
                    selectTargetChat(scrollAttempts + 1)
                }, 500)
            } else {
                // 已滚动到底部,仍未找到
                onChatNotFound(targetChat)
            }
        } else {
            // 找不到可滚动节点
            onChatNotFound(targetChat)
        }
    }
}
```

#### 方案2: 全角/半角字符智能匹配 ✅

实现文本标准化,自动处理全角/半角字符差异:

```kotlin
/**
 * 标准化文本:将全角字符转换为半角字符,用于模糊匹配
 */
private fun normalizeText(text: String): String {
    return text.map { char ->
        when (char) {
            // 全角波浪号 → 半角波浪号
            '～' -> '~'
            // 全角空格 → 半角空格
            '　' -> ' '
            // 全角数字 → 半角数字
            in '０'..'９' -> (char.code - '０'.code + '0'.code).toChar()
            // 全角字母 → 半角字母
            in 'Ａ'..'Ｚ' -> (char.code - 'Ａ'.code + 'A'.code).toChar()
            in 'ａ'..'ｚ' -> (char.code - 'ａ'.code + 'a'.code).toChar()
            else -> char
        }
    }.joinToString("")
}

/**
 * 查找包含指定文本的节点(支持全角/半角模糊匹配)
 */
private fun findNodeContainingText(node: AccessibilityNodeInfo, text: String): AccessibilityNodeInfo? {
    val nodeText = node.text?.toString() ?: ""

    // 标准化后进行比较
    val normalizedNodeText = normalizeText(nodeText)
    val normalizedSearchText = normalizeText(text)

    if (normalizedNodeText.contains(normalizedSearchText)) {
        return node
    }

    for (i in 0 until node.childCount) {
        val child = node.getChild(i) ?: continue
        val result = findNodeContainingText(child, text)
        if (result != null) return result
    }

    return null
}
```

### 关键技术点

1. **滚动查找**: 使用`ACTION_SCROLL_FORWARD`向下滚动,直到找到或到底部
2. **智能等待**: 滚动后等待500ms让页面加载完成
3. **递归查找**: 滚动后递归调用自己继续查找
4. **文本标准化**: 自动处理全角/半角字符差异,提高匹配成功率
5. **底部检测**: `performAction()`返回`false`说明已到底部,无法继续滚动

### 测试结果

- ✅ 成功找到页面下方的联系人
- ✅ 自动处理全角/半角波浪号差异
- ✅ 滚动到底部后正确报告未找到

---

## 📜 多选模式滚动问题

### 问题描述

在多选模式下,需要向上滚动加载更多消息,但手势滑动(`input swipe`)不起作用。

### 问题原因

企业微信在多选模式下禁用了手势滑动,只能通过AccessibilityService的滚动Action来实现。

### 解决方案

使用`ACTION_SCROLL_BACKWARD`向上滚动:

```kotlin
private fun scrollToLoadMoreMessages() {
    val rootNode = rootInActiveWindow ?: return

    // 查找可滚动节点
    val scrollableNode = findScrollableNode(rootNode)

    if (scrollableNode != null) {
        Log.e(TAG, "✅ 找到可滚动节点,使用ACTION_SCROLL_BACKWARD滚动")
        val scrolled = scrollableNode.performAction(AccessibilityNodeInfo.ACTION_SCROLL_BACKWARD)
        Log.e(TAG, "📜 滚动结果: $scrolled")

        if (scrolled) {
            // 等待滚动完成后继续勾选
            handler.postDelayed({
                continueSelectingMessages()
            }, 800)
        } else {
            // 无法滚动,可能已经到顶部
            Log.e(TAG, "⚠️ 无法继续滚动,可能已到顶部")
        }
    }
}

private fun findScrollableNode(node: AccessibilityNodeInfo): AccessibilityNodeInfo? {
    if (node.isScrollable) {
        return node
    }

    for (i in 0 until node.childCount) {
        val child = node.getChild(i) ?: continue
        val result = findScrollableNode(child)
        if (result != null) return result
    }

    return null
}
```

### 关键技术点

1. **使用AccessibilityAction**: `ACTION_SCROLL_BACKWARD`向上滚动,`ACTION_SCROLL_FORWARD`向下滚动
2. **查找可滚动节点**: 递归查找`isScrollable=true`的节点
3. **等待滚动完成**: 滚动后等待800ms让页面稳定
4. **检测滚动结果**: `performAction()`返回`false`说明无法继续滚动

### 为什么不用手势滑动?

```kotlin
// ❌ 这种方法在多选模式下不起作用
adb shell input swipe 360 1200 360 400 300

// ✅ 必须使用AccessibilityAction
scrollableNode.performAction(AccessibilityNodeInfo.ACTION_SCROLL_BACKWARD)
```

---

## 👆 逐条转发按钮点击问题

### 问题描述

"逐条转发"按钮的`clickable="false"`,使用`clickNode()`无法点击。

### 问题原因

企业微信的"逐条转发"按钮虽然可以点击,但在UI树中标记为`clickable="false"`,导致`performAction(ACTION_CLICK)`失败。

### 解决方案

使用坐标点击:

```kotlin
private fun clickOneByOneForward() {
    val rootNode = rootInActiveWindow ?: return
    val forwardButton = findNodeContainingText(rootNode, "逐条转发") ?: return

    // 获取按钮坐标
    val rect = android.graphics.Rect()
    forwardButton.getBoundsInScreen(rect)

    val centerX = (rect.left + rect.right) / 2
    val centerY = (rect.top + rect.bottom) / 2

    // 使用手势点击
    val path = android.graphics.Path()
    path.moveTo(centerX.toFloat(), centerY.toFloat())

    val gestureBuilder = android.accessibilityservice.GestureDescription.Builder()
    val strokeDescription = android.accessibilityservice.GestureDescription.StrokeDescription(path, 0, 100)
    gestureBuilder.addStroke(strokeDescription)

    dispatchGesture(gestureBuilder.build(), object : GestureResultCallback() {
        override fun onCompleted(gestureDescription: GestureDescription?) {
            Log.e(TAG, "✅ 点击'逐条转发'成功")
            // 继续下一步
        }
    }, null)
}
```

### 关键技术点

1. **动态坐标计算**: 根据按钮实际位置计算中心点
2. **GestureDescription**: 使用手势模拟点击
3. **回调处理**: 在`onCompleted`中继续下一步操作

---

## 📊 总结

### 核心技术要点

1. **精确节点查找**: 优先使用`resource-id`,避免文本查找的歧义
2. **双重保险策略**: 主方法+备用方案,提高成功率
3. **智能滚动查找**: 自动滚动直到找到目标或到底部
4. **文本标准化**: 处理全角/半角字符差异
5. **AccessibilityAction**: 在某些场景下比手势滑动更可靠
6. **动态坐标点击**: 当节点不可点击时的有效替代方案

### 调试技巧

1. **查看UI结构**: `adb shell uiautomator dump && adb pull /sdcard/window_dump.xml`
2. **查看日志**: `adb logcat -d | grep "BatchSendService"`
3. **测试滚动**: `adb shell input swipe x1 y1 x2 y2 duration`
4. **测试点击**: `adb shell input tap x y`

### 最佳实践

1. **优先使用resource-id查找节点**
2. **实现多层fallback机制**
3. **添加详细的日志输出**
4. **智能等待而非硬编码延迟**
5. **处理边界情况(如滚动到底部)**

---

## 🔀 功能二执行功能一脚本问题

### 问题描述

点击"功能二"的"开始批量发送"按钮后,双企微选择弹窗被点击,企微打开,但是执行的是"功能一"的脚本(导航到通讯录、打开添加客户、打开新的客户),而不是"功能二"的批量发送脚本。

### 问题原因

1. **WeworkAutoService处理双企微弹窗**: `WeworkAutoService`负责检测并点击双企微选择弹窗
2. **延迟回调导致冲突**: 点击弹窗后,`WeworkAutoService`设置了3秒延迟回调,调用`navigateToContacts()`(功能一的逻辑)
3. **没有区分功能**: `WeworkAutoService`没有检查是功能一还是功能二启动,统一执行功能一的流程

### 解决方案

#### 方案: 检查SharedPreferences区分功能 ✅

在`WeworkAutoService.clickWeworkByCoordinate()`中,点击弹窗后检查`shouldStartAuto`,只有功能一才设置延迟回调:

```kotlin
private fun clickWeworkByCoordinate(targetWework: String) {
    // ... 点击弹窗代码 ...

    if (clicked) {
        android.util.Log.e(TAG, "✅ 点击成功!")
        sendLog("✅ 已自动选择: $targetWework")

        // 🔥 检查是功能一还是功能二
        val prefsAuto = getSharedPreferences("wework_auto", android.content.Context.MODE_PRIVATE)
        val shouldStartAuto = prefsAuto.getBoolean("should_start", false)

        // 只有功能一才导航到通讯录,功能二让BatchSendService接管
        if (shouldStartAuto) {
            android.util.Log.e(TAG, "⏰ 功能一启动,3秒后导航到通讯录")
            handler.postDelayed({
                android.util.Log.e(TAG, "⏰ 3秒延迟结束,开始导航到通讯录")
                currentState = ProcessState.NAVIGATING_TO_CONTACTS
                navigateToContacts()
            }, 3000)
        } else {
            android.util.Log.e(TAG, "⏰ 功能二启动,不导航到通讯录,让BatchSendService接管")
        }
    }
}
```

同时在`WeworkAutoService.onAccessibilityEvent()`中,检测到企微事件时,如果是功能二启动,直接返回,不处理:

```kotlin
// 检查是否需要启动批量处理(只处理功能一,不处理功能二)
if (!isProcessing && event.packageName == "com.tencent.wework") {
    // 🔥 检查是否是功能二启动的
    val prefsBatch = getSharedPreferences("batch_send", android.content.Context.MODE_PRIVATE)
    val shouldStartBatch = prefsBatch.getBoolean("should_start", false)

    // 如果是功能二,不要处理,让BatchSendService处理
    if (shouldStartBatch) {
        android.util.Log.e("WEWORK_DEBUG", "⚠️ 功能二启动,WeworkAutoService不处理企微事件")
        return
    }

    android.util.Log.e("WEWORK_DEBUG", "🔍 检测到企业微信，检查是否需要启动批量处理...")
    checkAndStartBatchProcess()
}
```

### 关键技术点

1. **SharedPreferences通信**: 使用`wework_auto`和`batch_send`两个独立的SharedPreferences区分功能
2. **条件延迟回调**: 只有功能一才设置延迟回调,功能二不设置
3. **服务隔离**: `WeworkAutoService`只处理功能一,`BatchSendService`只处理功能二
4. **早期返回**: 在事件处理早期就检查并返回,避免冲突

### 测试结果

- ✅ 功能一正常工作,点击"开始批量处理"后自动导航到通讯录
- ✅ 功能二正常工作,点击"开始批量发送"后不导航到通讯录,由`BatchSendService`接管

---

## 📱 消息节点识别问题

### 问题描述

在素材库聊天中,`BatchSendService`无法找到消息节点,日志显示"找到消息节点数量: 0",导致无法长按消息进入多选模式。用户反馈:"只能识别别人发送过来的消息,自己发的消息识别不到"。

### 问题原因

1. **resource-id不匹配**: 原代码只查找以下resource-id:
   - `com.tencent.wework:id/hxd` - 别人发的文字消息
   - `com.tencent.wework:id/ih3` - 别人发的卡片消息
   - `com.tencent.wework:id/k2j` - 卡片消息中的图片

2. **自己发的消息结构不同**: 通过UI dump分析发现,自己发的消息使用不同的resource-id:
   - `com.tencent.wework:id/hwl` - 自己发的消息(LinearLayout, long-clickable="true")

### 解决方案

#### 方案: 添加自己发的消息的resource-id ✅

在`findMessageNodes()`方法中添加`com.tencent.wework:id/hwl`:

```kotlin
private fun findMessageNodes(node: AccessibilityNodeInfo?): List<AccessibilityNodeInfo> {
    val result = mutableListOf<AccessibilityNodeInfo>()
    if (node == null) return result

    // 查找所有可长按的消息节点
    // 包括: ih3, hxd (消息内容), k2j (卡片消息中的图片), hwl (自己发的消息)
    val resourceId = node.viewIdResourceName
    if (node.isLongClickable &&
        (resourceId == "com.tencent.wework:id/hxd" ||   // 文字消息(别人发的)
         resourceId == "com.tencent.wework:id/ih3" ||   // 卡片消息(LinearLayout)
         resourceId == "com.tencent.wework:id/k2j" ||   // 卡片消息中的图片(ImageView)
         resourceId == "com.tencent.wework:id/hwl")) {  // 🔥 自己发的消息(LinearLayout)
        result.add(node)
    }

    // 递归查找子节点
    for (i in 0 until node.childCount) {
        result.addAll(findMessageNodes(node.getChild(i)))
    }

    return result
}
```

### UI结构分析

通过`adb shell uiautomator dump`分析素材库聊天页面,发现自己发的消息结构:

```xml
<node resource-id="com.tencent.wework:id/hwl"
      class="android.widget.LinearLayout"
      long-clickable="true"
      bounds="[539,172][614,226]">
    <node resource-id="com.tencent.wework:id/idk"
          class="android.widget.TextView"
          text="1" />
</node>
```

关键特征:
- `resource-id="com.tencent.wework:id/hwl"`
- `long-clickable="true"`
- 包含TextView子节点显示消息内容

### 关键技术点

1. **UI dump分析**: 使用`uiautomator dump`查看实际UI结构
2. **区分消息类型**: 别人发的消息和自己发的消息使用不同的resource-id
3. **完整覆盖**: 同时支持接收消息和发送消息的识别
4. **long-clickable检查**: 确保节点可以长按

### 测试结果

- ✅ 成功识别自己发的消息(文本"1", "2", "3"等)
- ✅ 成功长按最后一条消息
- ✅ 进入多选模式

---

## 📊 更新总结

### 新增问题解决

1. **功能二执行功能一脚本**: 通过检查SharedPreferences区分功能,条件设置延迟回调
2. **消息节点识别**: 添加自己发的消息的resource-id支持

### 技术要点更新

1. **服务隔离**: 不同功能使用不同的AccessibilityService或通过SharedPreferences区分
2. **UI结构分析**: 使用uiautomator dump分析实际UI结构,找到正确的resource-id
3. **完整性测试**: 测试不同场景(别人发的消息、自己发的消息、不同消息类型)

